name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello World!"
          echo "This is my first GitHub Action"
      - name: Print goodbye
        run: |
          echo "Done - bye!"
          echo "See you later!"
